{
  "scripts": {
    "start": "cd frontend && npm start",
    "android": "cd frontend && npm run android",
    "ios": "cd frontend && npm run ios",
    "web": "cd frontend && npm run web",
    "build": "rm -rf frontend/dist && cd frontend && npm install && npx expo export --platform web && cd ../backend && npm install && npx prisma generate"
  },
  "dependencies": {
    "expo": "~54.0.30",
    "react-native-safe-area-context": "^5.6.2",
    "socket.io-client": "^4.8.1"
  },
  "main": "index.js",
  "devDependencies": {
    "@types/react": "~19.1.10",
    "typescript": "~5.9.2"
  }
}
